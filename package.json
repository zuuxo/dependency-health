{
  "name": "dependency-health",
  "version": "0.1.0",
  "description": "TypeScript CLI for auditing dependency freshness, duplication and lockfile health",
  "type": "module",
  "bin": { "dependency-health": "./dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "tsx --test tests/*.test.ts"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "commander": "^13.1.0",
    "semver": "^7.7.1",
    "yaml": "^2.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@types/semver": "^7.7.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "license": "MIT"
}
